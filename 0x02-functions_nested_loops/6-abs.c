#include<stdio.h>
#include"main.h"
/**
 * _abs - Entry point
 *
 * Description: 'the program's descriptor'
 * @n : int n
 * Return: Always 0 (Success)
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}
